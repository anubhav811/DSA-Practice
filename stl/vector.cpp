#include <bits/stdc++.h>
using namespace std;
int main()
{
    // array<int, 5> arr = {1, 3, 4, 5, 6};

    vector<int> arr;
    cout < arr.size() << endl;
    arr.push_back(0);
    arr.push_back(4);
    arr.push_back(4);
    cout < arr.size() << endl;

    return 0;
}